import { PageContent } from '../../components/UI/PageContent'
import { TableHeader } from '../../components/UI/TableHeader'

export const ScheduleSettings = (): JSX.Element => {
  return (
    <PageContent>
      <TableHeader title="Configurações de Agenda" margin="0 0 20px 0" />
    </PageContent>
  )
}
